services:
  - type: web
    name: fragility-signals
    runtime: python
    rootDir: apps/fragility-dashboard
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app --bind 0.0.0.0:$PORT
    plan: free
